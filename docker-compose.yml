version: '3.8'

services:
  prod:
    container_name: rayonweb-prod
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    restart: unless-stopped
    command: npm run start
    # No volumes in prod to avoid overwriting build artifacts like `.next` folder

volumes:
  node_modules:
